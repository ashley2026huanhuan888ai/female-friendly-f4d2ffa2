## 目标

让"有详细厌女描述、但 tags 为空"的观察也能让对象温度升上去。不再依赖管理员是否打标签。

## 改动范围

**只改一个文件**：`src/lib/temperature-rules.ts`

新增一组"厌女语义关键词 → 温度地板"映射，与现有的"法律/监管关键词 → 90°C"并列。`aggregateRuleMinimum` 已经扫描 `content / summary / cleaned_content / tags` 拼成的文本，无需改调用方。

## 关键词分级（命中即触发对应地板）

```text
极严重 → 地板 75°C
  迷奸、下药、性暴力、强奸文化、荡妇羞辱、性羞辱

严重 → 地板 60°C
  受害者归因、受害者有罪、活该被、自找的、
  生育规训、催生、不生孩子就、女人就该生
  容貌羞辱、身材羞辱、丑女、肥婆
  
中等 → 地板 45°C
  物化女性、女性工具化、男性凝视、奇观化、
  贤妻良母规训、为了儿子、为了丈夫、相夫教子、
  能力贬低、女人头发长见识短、女司机、
  性别角色固化、女主内、男主外
  
轻度 → 地板 35°C
  擦边营销、低俗营销、伪女性友好、girl power 营销话术、
  疯女人、歇斯底里、田园女权（贬义语境）
```

最终关键词清单在实现时定稿，原则：

- 描述性 + 中文为主，匹配真实观察文本（如"为了儿子/丈夫"、"疯姐姐"）
- 与 `knowledge_tags` 表的 canonical 名称对齐，复用已有词表
- 关键词命中是"地板"，不替代 AI 引擎；若 AI 引擎算得更高仍取高值

## 行为

```text
final = max(AI引擎, 法律地板, 厌女语义地板, 管理员地板)
```

- 命中多个关键词 → 取最高地板
- `triggered_rules` 数组里记录命中详情（如 `"语义命中: 受害者归因 → ≥60°C"`），用于详情页"温度构成"展示与调试

## 验证

1. 触发现有 `recomputeAndPersist` 重算（管理后台已有"扫描修复温度"按钮 `scanAndFixTemperatures`）
2. 验收：
   - 《相逢时节》观察 #2 命中"为了儿子/丈夫" + "奇观化" → 至少 45°C
   - 宝洁等仅有占位观察的对象不受影响（无关键词命中）
   - 椰树等 0 观察对象不受影响
3. 新增 `temperature-rules.test.ts` 用例：空 tags + 关键词命中 → 期望地板

## 不做的事

- 不改 UI（方案 D 留作下一轮）
- 不改审核流程
- 不改 AI 引擎公式
- 不批量改历史 tags，只让规则地板兜住

## 关键词清单确认

实现前我会先把完整中文关键词清单贴给你过目，确认无误再写入代码（避免误伤，比如"为了家"这种太宽泛的词不收录）。
