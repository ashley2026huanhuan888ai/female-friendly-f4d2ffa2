## 目标
在 `/points-system` 页面增加一个显眼的「邀请朋友注册」按钮，引导用户到已有的 `/contribution` 页面（那里已显示个人邀请码、邀请链接复制按钮以及直接邀请列表）。

## 改动
仅编辑 `src/routes/points-system.tsx`：

1. 在「核心逻辑」卡片下方（约第 41 行后）新增一个 CTA 区块：
   - 主按钮：`<Link to="/contribution">立即邀请朋友 →</Link>`，使用 `bg-accent text-accent-foreground` 芭比粉主色样式。
   - 副文案：「登录后获取你的专属邀请码与链接，每邀请一位好友注册得 5 分，好友贡献还有 10% 返利。」
2. 在页面底部「邀请绑定」说明附近再放一个次级 `Link`（轻量样式），方便看完规则后直接行动。

## 不动的范围
- 不修改邀请逻辑、不新增路由、不改后端、不改 `/contribution` 页面。
- 仅前端展示层改动。
