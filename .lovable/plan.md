## 改动
- 文件：`src/routes/index.tsx`（hero 区块）
- 在 eyebrow `Observatory · Est. 2026` 下方、H1 标题之前，新增一行链接：
  - 文案：`了解平台如何判断 →`（用 `t("nav.howWeJudge")` 复用现有 i18n）
  - 链接到 `/how-we-judge`
  - 样式：小号、accent 芭比粉、hover 下划线，与 eyebrow 视觉分量呼应而稍突出，作为 hero 的第二行视觉锚点

## 不改动
- 不动 H1、步骤卡片、正文段落、搜索框等其它 hero 内容。
