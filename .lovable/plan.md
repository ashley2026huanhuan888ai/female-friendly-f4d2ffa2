1. 在 `src/routes/index.tsx` 的首页 Hero 左侧内容中，把说明文字、搜索框、CTA 按钮组统一放进同一个受控宽度容器。
2. 将该容器设置为靠左展示，并限制最大宽度，避免内容延伸到截图绿色区域右侧之外；目标是整体落在蓝色区域内。
3. 优化移动端搜索框和按钮组：保持搜索栏不横向溢出，CTA 可换行但不超出容器边界。
4. 仅调整这个首屏布局，不改其他页面、文案或功能。