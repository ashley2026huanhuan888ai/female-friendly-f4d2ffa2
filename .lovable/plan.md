原因基本不在网络：当前 `toPng` 失败抛出的是 `{ isTrusted: true }`，这是图片/资源元素触发的 `error` 事件；更可能是离屏导出节点里仍保留了失败截图、以及 `tempBand.color` 使用了 CSS 变量/oklch 颜色，html-to-image 在 SVG/canvas 转换时兼容性差。

计划：
1. 把导出卡片里的温度颜色改成导出安全的 HEX 固定色，避免 CSS 变量/oklch 进入 html-to-image。
2. 截图预取失败时，不再把原始截图 URL 留在离屏导出节点里，而是导出时自动跳过失败截图，只保留文字内容。
3. 等待图片加载时记录失败图片数量，并在失败后提示「部分截图无法加入，已尝试仅导出文字/可用截图」。
4. 重试策略升级：第一次完整导出，第二次跳过失败截图，第三次进一步降低像素比；若仍失败，提示明确原因不是网络而是当前截图/浏览器导出兼容问题。
5. 同步更新中英文提示文案。