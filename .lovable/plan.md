## 目标
手机端对象详情页上下堆叠布局更紧凑美观，温度计与"最新观察"之间不要留太大空白。

## 调整（仅 `src/routes/objects.$id.tsx` 头部区块）

1. 外层 grid：`py-16` → 手机 `py-8`，桌面保持 `md:py-24`；`gap-6` → 手机 `gap-4`。
2. 温度计行（order-1）：手机端从左对齐改为水平排布、温度计稍微缩小或保持 `lg`，加 `pb-2 border-b border-border/60` 作为分隔，使其与下方文字区有清晰但紧凑的过渡；"为什么是这个温度?" 按钮 `mt-3` → `mt-1`。
3. 文字区（order-2）：顶部 meta 行 `mt-0`；标题 `mt-4` → `mt-3`；"最新观察" 区块 `mt-10` → `mt-6`，标签与正文之间 `mt-3` → `mt-2`。
4. 桌面端（md+）保留原间距，全部用 `md:` 前缀覆盖回原值，避免影响桌面视觉。

不动文案、组件结构、数据逻辑。